#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: first integer to be checked
 * @b: second intger to be checked
 * Return: 0 (success)
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
